<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
   <%= link_to t('links.edit', name: Book.model_name.human.downcase), edit_book_path(@book) %> |
   <%= link_to t('links.index', name: Book.model_name.human.downcase), books_path %>
   <%= button_to t('links.destroy', name: Book.model_name.human.downcase), @book, method: :delete %>
</nav>
