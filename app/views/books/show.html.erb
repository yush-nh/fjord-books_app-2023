<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
   <%= link_to t('books.links.edit'), edit_book_path(@book) %> |
   <%= link_to t('books.links.index'), books_path %>
   <%= button_to t('books.links.delete'), @book, method: :delete %>
</nav>
